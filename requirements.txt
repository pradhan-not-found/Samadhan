# Samadhan backend dependencies
fastapi>=0.128
uvicorn>=0.40
sqlalchemy>=2.0
pydantic>=2.0
python-multipart

# Gemma 4 inference
kagglehub>=1.0
transformers>=5.0
accelerate
bitsandbytes
pillow

# PyTorch — install the CUDA build separately for GPU inference:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch
